<!--Root -->
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <Nullable>enable</Nullable>
    <LangVersion>8.0</LangVersion>
    <ArtifactsDir>$(MSBuildStartupDirectory)/.artifacts</ArtifactsDir>
  </PropertyGroup>

  <PropertyGroup>
    <BaseIntermediateOutputPath>$(ArtifactsDir)/build/obj/$(MSBuildProjectName)/</BaseIntermediateOutputPath>
    <BaseOutputPath>$(ArtifactsDir)/build/bin/$(MSBuildProjectName)/</BaseOutputPath>
  </PropertyGroup>

  <Import Project="Version.props" />
</Project>
